import type { Metadata } from "next";
import { Geist, Geist_Mono, Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "IELTS Course by Munzereen Shahid - 10 Minute School",
  description: "Complete IELTS preparation course with expert guidance",
  keywords:
    "IELTS, English, Course, Preparation, Munzereen Shahid, 10 Minute School",
  authors: [{ name: "10 Minute School" }],
  viewport: "width=device-width, initial-scale=1",
  robots: "index, follow",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
